"""Maker Studio domains: the library-specific plug-ins for the shared engine.

Each turns a brief into code the brain writes, executes it (sandboxed) into a
real artifact, and checks it — the engine (studio.engine) owns the shared
generate → execute → self-correct → version loop.
"""
